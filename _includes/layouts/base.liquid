<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--
    For data that affects the whole site (say, metadata that describes it), we
    can add global data to 11ty's data folder! Feel free to expand the included
    `metadata.json` file with any other information you deem necessary!
    (https://www.11ty.dev/docs/data-global/)
    -->
    <title>{{ metadata.title }}
      {%- if title %}
        | {{ title }}
      {% endif %}
    </title>
    <link rel="icon" 
      type="image/png" 
      href="/img/favicon.png">
    <link href="/css/style.css"
      rel="stylesheet"
      type="text/css"
      media="all" />

    <link href="https://fonts.googleapis.com/css2?family=Darumadrop+One&family=Comic+Neue:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&family=Yellowtail&display=swap" rel="stylesheet">
    </head>
  <body>

<div class="buttons">
      <div class="theme-switcher">
        <button data-theme="light" aria-pressed="false">🌞</button>
        <button data-theme="dark" aria-pressed="true">🌑</button>
      </div>
      <span>Theme</span>
      </div>      


    {%# We use `render` tags to include other liquid files in our template %}
    {% render 'header.liquid' %}
    {% comment %}
    Since this file is used as a layout, we use the `content` variable to mark
    where the contents of pages that use this layout go!
    (https://www.11ty.dev/docs/layouts/)
    {% endcomment %}
    <main>
    {{ content }}
    </main>
    {% render 'footer.liquid' %}
    
    

  <script type="text/javascript" src="/js/themeswitcher.js"></script>
  </body>
</html>